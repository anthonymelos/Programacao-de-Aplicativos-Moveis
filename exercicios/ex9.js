/*Calculadora como Classe
Crie uma classe Calculadora com métodos:

somar(a, b), subtrair(a, b), multiplicar(a, b), dividir(a, b)
calcular(a, operacao, b) que chama o método correto baseado na operação
Adicione validação para divisão por zero

javascript// Teste:
const calc = new Calculadora();
console.log(calc.somar(5, 3)); // 8
console.log(calc.calcular(10, "/", 2)); // 5
console.log(calc.calcular(10, "/", 0)); // "Erro: Divisão por zero"
*/